openapi: 3.1.0
info:
  title: Experimente+ API
  version: 0.1.0
  description: |
    ## Experimente+ API

    API da plataforma regional Experimente+, construída com AdonisJS 7, React e Inertia. Neste estágio inicial, o contrato documenta a fundação compartilhada de identidade, autorização, workspaces, arquivos e saúde da aplicação. Os domínios específicos do produto serão adicionados após o planejamento funcional.

    ### Foundation capabilities

    - **Access JWTs**: Short-lived signed bearer tokens for API requests
    - **Opaque Refresh Tokens**: Rotated on every use and persisted only as HMAC hashes
    - **Global RBAC**: Roles, direct permissions, inheritance, and cache invalidation
    - **Tenant Workspaces**: Verified active-tenant context, personal workspace onboarding, and workspace creation
    - **File Management**: Tenant-scoped upload, listing, and owner-aware deletion across multiple storage drivers
    - **Account Lifecycle**: Email verification, single-use password reset, and anonymized self-deletion
    - **Health Monitoring**: Public readiness and dependency checks

    ## Authentication

    1. **Login**: POST to `/api/v1/sessions/sign-in` with your credentials
    2. **Get Tokens**: Extract `access_token` and `refresh_token` from `auth`
    3. **Use Access Token**: Include `Authorization: Bearer <access_token>`
    4. **Rotate Tokens**: POST the opaque refresh token to `/api/v1/sessions/refresh`

    ### Token Management

    - **API Access Token Lifetime**: 15 minutes
    - **Web Cookie Lifetime**: 1 hour
    - **Refresh Token Lifetime**: 3 days
    - **Access Token Format**: JWT with `token_use=access`
    - **Refresh Token Format**: Opaque random value; it is not a JWT

    ## API Conventions

    ### Request Format
    - **Content-Type**: `application/json` for all requests (except file uploads)
    - **Pagination**: Use `?page=1&per_page=10` query parameters
    - **Sorting**: Use `?sort_by=field&order=asc` or `?sort_by=field&order=desc`
    - **Searching**: Use `?search=term` for text search

    ### Response Format
    - **Success**: HTTP 2xx with JSON body
    - **Errors**: HTTP 4xx/5xx with standardized error format
    - **Timestamps**: ISO 8601 format
    - **Database Fields**: snake_case naming convention

    ### Common Headers
    ```
    Authorization: Bearer <token>
    Content-Type: application/json
    Accept: application/json
    ```

  contact:
    name: Experimente+ Engineering
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://127.0.0.1:3333
    description: Local development server
  - url: https://api.example.com
    description: Production placeholder (replace when the public domain is defined)

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Short-lived access JWT. Opaque refresh tokens are never accepted here.

  schemas:
    Region:
      type: object
      required: [id, tenant_id, name, slug, sort_order, is_active]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        sort_order:
          type: integer
        is_active:
          type: boolean
        cities:
          type: array
          items:
            $ref: '#/components/schemas/City'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    City:
      type: object
      required:
        [
          id,
          tenant_id,
          region_id,
          name,
          slug,
          state_code,
          country_code,
          timezone,
          sort_order,
          is_active,
        ]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        region_id:
          type: integer
        name:
          type: string
        slug:
          type: string
        state_code:
          type: string
          minLength: 2
          maxLength: 2
        country_code:
          type: string
          minLength: 2
          maxLength: 2
        ibge_code:
          type: [string, 'null']
        timezone:
          type: string
          example: America/Sao_Paulo
        latitude:
          type: [number, 'null']
          format: double
        longitude:
          type: [number, 'null']
          format: double
        sort_order:
          type: integer
        is_active:
          type: boolean
        region:
          oneOf:
            - $ref: '#/components/schemas/Region'
            - type: 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CategoryFamily:
      type: object
      required: [id, tenant_id, name, slug, sort_order, is_active]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        icon:
          type: [string, 'null']
        sort_order:
          type: integer
        is_active:
          type: boolean
        categories:
          type: array
          items:
            $ref: '#/components/schemas/Category'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    Category:
      type: object
      required: [id, tenant_id, family_id, name, slug, sort_order, is_active]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        family_id:
          type: integer
        parent_id:
          type: [integer, 'null']
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        icon:
          type: [string, 'null']
        sort_order:
          type: integer
        is_active:
          type: boolean
        children:
          type: array
          items:
            $ref: '#/components/schemas/Category'
        attribute_definitions:
          type: array
          items:
            $ref: '#/components/schemas/CategoryAttributeDefinition'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CategoryAttributeDefinition:
      type: object
      required:
        [
          id,
          tenant_id,
          category_id,
          key,
          name,
          data_type,
          is_required,
          is_filterable,
          is_public,
          applies_to_descendants,
          sort_order,
          is_active,
        ]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        category_id:
          type: integer
        key:
          type: string
        name:
          type: string
        description:
          type: [string, 'null']
        data_type:
          type: string
          enum: [text, long_text, boolean, integer, decimal, single_select, multi_select, url]
        unit:
          type: [string, 'null']
        validation_rules:
          type: object
          additionalProperties: true
        is_required:
          type: boolean
        is_filterable:
          type: boolean
        is_public:
          type: boolean
        applies_to_descendants:
          type: boolean
        sort_order:
          type: integer
        is_active:
          type: boolean
        options:
          type: array
          items:
            $ref: '#/components/schemas/CategoryAttributeOption'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CategoryAttributeOption:
      type: object
      required: [id, tenant_id, attribute_definition_id, label, value, sort_order, is_active]
      properties:
        id:
          type: integer
        tenant_id:
          type: integer
        attribute_definition_id:
          type: integer
        label:
          type: string
        value:
          type: string
        sort_order:
          type: integer
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateRegionInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    CreateCityInput:
      type: object
      required: [region_id, name, state_code, timezone]
      properties:
        region_id:
          type: integer
        name:
          type: string
        slug:
          type: string
        state_code:
          type: string
        country_code:
          type: string
          default: BR
        ibge_code:
          type: [string, 'null']
        timezone:
          type: string
          example: America/Sao_Paulo
        latitude:
          type: [number, 'null']
        longitude:
          type: [number, 'null']
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    CreateCategoryFamilyInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        icon:
          type: [string, 'null']
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    CreateCategoryInput:
      type: object
      required: [family_id, name]
      properties:
        family_id:
          type: integer
        parent_id:
          type: [integer, 'null']
        name:
          type: string
        slug:
          type: string
        description:
          type: [string, 'null']
        icon:
          type: [string, 'null']
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    CreateCategoryAttributeDefinitionInput:
      type: object
      required: [category_id, key, name, data_type]
      properties:
        category_id:
          type: integer
        key:
          type: string
        name:
          type: string
        description:
          type: [string, 'null']
        data_type:
          type: string
          enum: [text, long_text, boolean, integer, decimal, single_select, multi_select, url]
        unit:
          type: [string, 'null']
        is_required:
          type: boolean
        is_filterable:
          type: boolean
        is_public:
          type: boolean
        applies_to_descendants:
          type: boolean
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    CreateCategoryAttributeOptionInput:
      type: object
      required: [attribute_definition_id, label]
      properties:
        attribute_definition_id:
          type: integer
        label:
          type: string
        value:
          type: string
        sort_order:
          type: integer
          minimum: 0
        is_active:
          type: boolean

    User:
      type: object
      properties:
        id:
          type: integer
          example: 1
        full_name:
          type: string
          example: 'John Doe'
        email:
          type: string
          format: email
          example: 'john@example.com'
        username:
          type: [string, 'null']
          example: 'johndoe'
        email_verified:
          type: boolean
          example: true
        email_verified_at:
          type: [string, 'null']
          format: date-time
        created_at:
          type: string
          format: date-time
          example: '2024-01-01T00:00:00.000Z'
        updated_at:
          type: string
          format: date-time
          example: '2024-01-01T00:00:00.000Z'
        roles:
          type: array
          items:
            $ref: '#/components/schemas/Role'

    Role:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: 'User'
        description:
          type: string
          example: 'Regular user role'
        slug:
          type: string
          enum: [root, admin, user, guest, editor]
          example: 'user'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    AuthResponse:
      type: object
      properties:
        id:
          type: integer
        full_name:
          type: string
        email:
          type: string
          format: email
        username:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        roles:
          type: array
          items:
            $ref: '#/components/schemas/Role'
        auth:
          $ref: '#/components/schemas/AuthTokens'
        email_verification_sent:
          type: boolean
          description: Present on sign-up responses; false means the account was created but email delivery failed.

    AuthTokens:
      type: object
      required: [access_token, refresh_token]
      properties:
        access_token:
          type: string
          description: Short-lived JWT accepted by bearer-authenticated endpoints.
          example: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
        refresh_token:
          type: string
          description: Opaque one-time refresh credential rotated after every successful refresh.
          example: 'D4m6j6pTW1u3Jf_Z0Q4K7rJch7Jv8k9xQ4Q6xk2MsGv...'

    RefreshTokenRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token:
          type: string
          minLength: 32
          description: Opaque refresh token returned by sign-in, sign-up, tenant switch, or refresh.

    PasswordResetRequest:
      type: object
      required: [token, password, password_confirmation]
      properties:
        token:
          type: string
          minLength: 32
          description: Opaque single-use password reset token delivered by email.
        password:
          type: string
          format: password
          minLength: 8
        password_confirmation:
          type: string
          format: password
          minLength: 8

    Tenant:
      type: object
      required: [id, name, slug, role]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        is_active:
          type: boolean
        role:
          type: string
          enum: [owner, admin, member]

    FileListItem:
      type: object
      required: [id, client_name, file_name, file_size, file_type, file_category, url, owner]
      properties:
        id:
          type: integer
        client_name:
          type: string
        file_name:
          type: string
        file_size:
          type: integer
        file_type:
          type: string
        file_category:
          type: string
          enum: [image, document, video, audio, file]
        url:
          type: string
        owner:
          type: object
          required: [id, full_name]
          properties:
            id:
              type: integer
            full_name:
              type: string
        created_at:
          type: [string, 'null']
          format: date-time

    Error:
      type: object
      properties:
        errors:
          type: array
          items:
            type: object
            properties:
              message:
                type: string
                example: 'Validation error'
              field:
                type: string
                example: 'email'
              rule:
                type: string
                example: 'email'

    Pagination:
      type: object
      properties:
        meta:
          type: object
          properties:
            total:
              type: integer
              example: 100
            per_page:
              type: integer
              example: 10
            current_page:
              type: integer
              example: 1
            last_page:
              type: integer
              example: 10
            first_page:
              type: integer
              example: 1
            first_page_url:
              type: string
              example: '/?page=1'
            last_page_url:
              type: string
              example: '/?page=10'
            next_page_url:
              type: string
              example: '/?page=2'
            previous_page_url:
              type: string
              example: null

    FileUploadResponse:
      type: object
      properties:
        url:
          type: string
          example: 'https://storage.example.com/files/abc123.pdf'
        clientName:
          type: string
          example: 'document.pdf'
        fileCategory:
          type: string
          enum: [image, document, video, audio, file]
          example: 'document'
        fileType:
          type: string
          example: 'application/pdf'
        size:
          type: integer
          example: 12345
        extname:
          type: string
          example: 'pdf'

  parameters:
    pathId:
      name: id
      in: path
      required: true
      schema:
        type: integer
      description: Resource ID

    pageParam:
      name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
      description: Page number for pagination

    perPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        default: 10
        minimum: 1
        maximum: 100
      description: Number of items per page

    sortByParam:
      name: sort_by
      in: query
      schema:
        type: string
        default: id
      description: Field to sort by

    orderParam:
      name: order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc
      description: Sort order

    searchParam:
      name: search
      in: query
      schema:
        type: string
      description: Search term for filtering results

    tenantHeader:
      name: x-tenant-id
      in: header
      required: false
      schema:
        type: integer
        minimum: 1
      description: Optional active workspace override. Membership and active status are always validated.

  responses:
    UnauthorizedError:
      description: Authentication required
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            errors:
              - message: 'Unauthorized'

    ForbiddenError:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            errors:
              - message: 'Forbidden'

    NotFoundError:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            errors:
              - message: 'Resource not found'

    ValidationError:
      description: Validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

tags:
  - name: Health
    description: System health monitoring endpoints
  - name: Authentication
    description: User authentication and session management
  - name: Users
    description: User management operations
  - name: Tenants
    description: Tenant membership and active workspace operations
  - name: Roles
    description: Role management operations (admin only)
  - name: Files
    description: File upload and management
  - name: Profile
    description: Current user profile operations
  - name: Permissions
    description: Permission management operations (admin only)

paths:
  /api/v1/catalog/regions:
    get:
      operationId: listPublicRegions
      summary: List active public regions
      tags: [Catalog]
      security: []
      responses:
        '200':
          description: Active regions with their active cities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Region'
        '404':
          description: Public operation could not be resolved

  /api/v1/catalog/cities:
    get:
      operationId: listPublicCities
      summary: List active public cities
      tags: [Catalog]
      security: []
      parameters:
        - in: query
          name: region_slug
          schema:
            type: string
          description: Optional active region slug
      responses:
        '200':
          description: Active cities for the resolved operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/City'
        '404':
          description: Public operation could not be resolved

  /api/v1/catalog/cities/{citySlug}:
    get:
      operationId: getPublicCity
      summary: Get an active public city by slug
      tags: [Catalog]
      security: []
      parameters:
        - in: path
          name: citySlug
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Public city
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/City'
        '404':
          description: City or public operation not found

  /api/v1/catalog/categories:
    get:
      operationId: listPublicTaxonomy
      summary: List the active public taxonomy tree
      tags: [Catalog]
      security: []
      responses:
        '200':
          description: Active category families, categories, attributes, and options
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryFamily'
        '404':
          description: Public operation could not be resolved

  /api/v1/admin/geography/regions:
    get:
      operationId: listRegions
      summary: List regions in the active operation
      tags: [Geography Admin]
      parameters:
        - in: query
          name: include_inactive
          schema:
            type: boolean
      responses:
        '200':
          description: Tenant-scoped regions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Region'
        '401':
          description: Authentication required
        '403':
          description: Insufficient permission or inaccessible operation
    post:
      operationId: createRegion
      summary: Create a region in the active operation
      tags: [Geography Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRegionInput'
      responses:
        '201':
          description: Region created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Region'
        '400':
          description: Domain rule rejected the request
        '401':
          description: Authentication required
        '403':
          description: Insufficient permission
        '422':
          description: Validation failed

  /api/v1/admin/geography/regions/{id}:
    get:
      operationId: getRegion
      summary: Get a region in the active operation
      tags: [Geography Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Region
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Region'
        '404':
          description: Region not found
    put:
      operationId: updateRegion
      summary: Update mutable region fields
      tags: [Geography Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Any mutable subset of CreateRegionInput
              additionalProperties: true
      responses:
        '200':
          description: Region updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Region'
        '400':
          description: Domain rule rejected the request
        '404':
          description: Region not found
        '422':
          description: Validation failed

  /api/v1/admin/geography/cities:
    get:
      operationId: listCities
      summary: List cities in the active operation
      tags: [Geography Admin]
      parameters:
        - in: query
          name: region_id
          schema:
            type: integer
        - in: query
          name: include_inactive
          schema:
            type: boolean
      responses:
        '200':
          description: Tenant-scoped cities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/City'
    post:
      operationId: createCity
      summary: Create a city in the active operation
      tags: [Geography Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCityInput'
      responses:
        '201':
          description: City created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/City'
        '400':
          description: Invalid timezone, coordinates, region, or uniqueness rule
        '422':
          description: Validation failed

  /api/v1/admin/geography/cities/{id}:
    get:
      operationId: getCity
      summary: Get a city in the active operation
      tags: [Geography Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: City
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/City'
        '404':
          description: City not found
    put:
      operationId: updateCity
      summary: Update mutable city fields
      tags: [Geography Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Any mutable subset of CreateCityInput
              additionalProperties: true
      responses:
        '200':
          description: City updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/City'
        '400':
          description: Domain rule rejected the request
        '404':
          description: City not found
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/families:
    get:
      operationId: listCategoryFamilies
      summary: List category families in the active operation
      tags: [Taxonomy Admin]
      responses:
        '200':
          description: Tenant-scoped category families
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryFamily'
    post:
      operationId: createCategoryFamily
      summary: Create a category family
      tags: [Taxonomy Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryFamilyInput'
      responses:
        '201':
          description: Category family created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryFamily'
        '400':
          description: Slug or domain rule rejected the request
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/families/{id}:
    get:
      operationId: getCategoryFamily
      summary: Get a category family
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Category family
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryFamily'
        '404':
          description: Category family not found
    put:
      operationId: updateCategoryFamily
      summary: Update mutable category-family fields
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Category family updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryFamily'
        '404':
          description: Category family not found
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/categories:
    get:
      operationId: listCategories
      summary: List categories in the active operation
      tags: [Taxonomy Admin]
      parameters:
        - in: query
          name: family_id
          schema:
            type: integer
        - in: query
          name: parent_id
          schema:
            type: integer
        - in: query
          name: include_inactive
          schema:
            type: boolean
      responses:
        '200':
          description: Tenant-scoped categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Category'
    post:
      operationId: createCategory
      summary: Create a root category or subcategory
      tags: [Taxonomy Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryInput'
      responses:
        '201':
          description: Category created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          description: Invalid family, parent, depth, or slug
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/categories/{id}:
    get:
      operationId: getCategory
      summary: Get a category with its relations
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Category
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '404':
          description: Category not found
    put:
      operationId: updateCategory
      summary: Update mutable category fields
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Category updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          description: Invalid hierarchy or domain rule
        '404':
          description: Category not found
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/attribute-definitions:
    get:
      operationId: listCategoryAttributeDefinitions
      summary: List category attribute definitions
      tags: [Taxonomy Admin]
      parameters:
        - in: query
          name: category_id
          schema:
            type: integer
      responses:
        '200':
          description: Tenant-scoped attribute definitions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryAttributeDefinition'
    post:
      operationId: createCategoryAttributeDefinition
      summary: Create a typed category attribute
      tags: [Taxonomy Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryAttributeDefinitionInput'
      responses:
        '201':
          description: Attribute definition created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeDefinition'
        '400':
          description: Invalid category, key, or filterability rule
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/attribute-definitions/{id}:
    get:
      operationId: getCategoryAttributeDefinition
      summary: Get an attribute definition with options
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Attribute definition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeDefinition'
        '404':
          description: Attribute definition not found
    put:
      operationId: updateCategoryAttributeDefinition
      summary: Update mutable attribute metadata
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Attribute definition updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeDefinition'
        '404':
          description: Attribute definition not found
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/attribute-options:
    get:
      operationId: listCategoryAttributeOptions
      summary: List options for a select attribute
      tags: [Taxonomy Admin]
      parameters:
        - in: query
          name: attribute_definition_id
          schema:
            type: integer
      responses:
        '200':
          description: Tenant-scoped options
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryAttributeOption'
    post:
      operationId: createCategoryAttributeOption
      summary: Create an option for a select attribute
      tags: [Taxonomy Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryAttributeOptionInput'
      responses:
        '201':
          description: Attribute option created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeOption'
        '400':
          description: Definition is not selectable or option value is invalid
        '422':
          description: Validation failed

  /api/v1/admin/taxonomy/attribute-options/{id}:
    get:
      operationId: getCategoryAttributeOption
      summary: Get an attribute option
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Attribute option
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeOption'
        '404':
          description: Attribute option not found
    put:
      operationId: updateCategoryAttributeOption
      summary: Update mutable option fields
      tags: [Taxonomy Admin]
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Attribute option updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryAttributeOption'
        '404':
          description: Attribute option not found
        '422':
          description: Validation failed
  /version:
    get:
      operationId: getApiInfo
      summary: Get API information
      description: Returns basic information about the API
      tags:
        - Health
      security: []
      responses:
        '200':
          description: API information
          content:
            application/json:
              schema:
                type: object
                properties:
                  name:
                    type: string
                    example: 'experimente-plus'
                  description:
                    type: string
                    example: 'Plataforma regional para descobrir lugares, experiências e negócios locais.'
                  version:
                    type: string
                    example: '0.1.0'
                  author:
                    type: string
                    example: 'Gabriel Maia'
                  contributors:
                    type: array
                    items:
                      type: string
        '429':
          description: Too Many Requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/health:
    get:
      operationId: healthCheck
      summary: Health check
      description: Check the health status of the API and its services
      tags:
        - Health
      security: []
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  healthy:
                    type: boolean
                    example: true
                  services:
                    type: object
                    properties:
                      database:
                        type: object
                        properties:
                          healthy:
                            type: boolean
                            example: true
        '429':
          description: Too Many Requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '503':
          description: Service unhealthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  healthy:
                    type: boolean
                    example: false
                  services:
                    type: object

  /api/v1/sessions/sign-in:
    post:
      operationId: signIn
      summary: User login
      description: Authenticate a user and receive a short-lived access JWT plus an opaque rotating refresh token.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - uid
                - password
              properties:
                uid:
                  type: string
                  description: Email or username
                  example: 'john@example.com'
                password:
                  type: string
                  format: password
                  minLength: 8
                  example: 'password123'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                errors:
                  - message: 'Invalid credentials'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/sessions/forgot-password:
    post:
      operationId: requestPasswordReset
      summary: Request a password reset link
      description: Returns the same accepted response whether or not the email exists. Any issued token is opaque, single-use, and stored only as an HMAC.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
      responses:
        '202':
          description: Request accepted without revealing account existence
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          description: Too many password reset requests

  /api/v1/sessions/reset-password:
    post:
      operationId: resetPassword
      summary: Reset an account password
      description: Consumes a valid reset token, changes the password, invalidates other reset links, and revokes active API refresh tokens.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequest'
      responses:
        '200':
          description: Password reset successfully
        '400':
          description: Invalid or expired token
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          description: Too many password reset attempts

  /api/v1/sessions/refresh:
    post:
      operationId: refreshSession
      summary: Rotate a refresh token
      description: Atomically revokes the submitted opaque refresh token and returns a new access/refresh pair.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: Token pair rotated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  auth:
                    $ref: '#/components/schemas/AuthTokens'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/sessions/logout:
    post:
      operationId: revokeSession
      summary: Revoke an API refresh token
      description: Revokes the submitted refresh token. Repeating the request is idempotent.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '204':
          description: Refresh token revoked
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/me:
    get:
      operationId: getProfile
      summary: Get current user profile
      description: Retrieve the profile of the currently authenticated user
      tags:
        - Profile
      responses:
        '200':
          description: Profile retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

    delete:
      operationId: deleteOwnAccount
      summary: Delete the authenticated account
      description: Requires the current password and the literal confirmation DELETE. The user is anonymized and active API credentials are revoked while historical references remain intact.
      tags:
        - Profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, confirmation]
              properties:
                current_password:
                  type: string
                  format: password
                confirmation:
                  type: string
                  const: DELETE
      responses:
        '204':
          description: Account anonymized and credentials revoked
        '400':
          description: Incorrect password or confirmation
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/me/permissions:
    get:
      operationId: getMyPermissions
      summary: Get current user permissions
      description: Retrieve the permissions of the currently authenticated user
      tags:
        - Profile
      responses:
        '200':
          description: Permissions retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  permissions:
                    type: array
                    items:
                      type: string
                      example: 'users.create'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/v1/me/roles:
    get:
      operationId: getMyRoles
      summary: Get current user roles
      description: Retrieve the roles of the currently authenticated user
      tags:
        - Profile
      responses:
        '200':
          description: Roles retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      $ref: '#/components/schemas/Role'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/v1/sessions/sign-up:
    post:
      operationId: signUp
      summary: User registration
      description: Creates a user, assigns the default role, optionally creates a personal workspace according to `REGISTRATION_WORKSPACE_MODE`, and returns tenant-scoped API tokens. Email delivery failure does not roll back the account.
      tags:
        - Authentication
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - full_name
                - email
                - password
                - password_confirmation
              properties:
                full_name:
                  type: string
                  example: 'John Doe'
                email:
                  type: string
                  format: email
                  example: 'john@example.com'
                username:
                  type: string
                  example: 'johndoe'
                  description: Optional username
                password:
                  type: string
                  format: password
                  minLength: 8
                  example: 'password123'
                password_confirmation:
                  type: string
                  format: password
                  example: 'password123'
      responses:
        '201':
          description: Registration successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/verify-email:
    get:
      operationId: verifyEmail
      summary: Verify user email
      description: Verify a user's email address using a token from the verification link.
      tags:
        - Authentication
      security: []
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
          description: The email verification token
      responses:
        '200':
          description: Email verified successfully
        '400':
          description: Invalid or expired token

  /api/v1/resend-verification-email:
    post:
      operationId: resendVerificationEmail
      summary: Resend verification email
      description: Resends the email verification link to the authenticated user.
      tags:
        - Authentication
      responses:
        '200':
          description: Verification email sent successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '503':
          description: Verification email could not be delivered

  /api/v1/tenants:
    post:
      operationId: createTenant
      summary: Create a workspace
      description: Creates an active workspace, adds the authenticated user as owner, and returns a token pair carrying the new tenant id. Requires `tenants.create`.
      tags:
        - Tenants
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 120
                  example: Acme Workspace
      responses:
        '201':
          description: Workspace created
          content:
            application/json:
              schema:
                type: object
                properties:
                  tenant:
                    $ref: '#/components/schemas/Tenant'
                  auth:
                    $ref: '#/components/schemas/AuthTokens'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/tenants/me:
    get:
      operationId: listMyTenants
      summary: List the authenticated user's tenant memberships
      tags:
        - Tenants
      responses:
        '200':
          description: Tenant memberships retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        name:
                          type: string
                        slug:
                          type: string
                        is_active:
                          type: boolean
                        role:
                          type: string
                          enum: [owner, admin, member]
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/v1/tenants/switch:
    post:
      operationId: switchTenant
      summary: Switch the active API tenant
      description: Validates active membership and issues a fresh access/refresh pair carrying the selected tenant.
      tags:
        - Tenants
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tenant_id]
              properties:
                tenant_id:
                  type: integer
                  minimum: 1
      responses:
        '200':
          description: Active tenant switched
          content:
            application/json:
              schema:
                type: object
                properties:
                  tenant:
                    type: object
                  auth:
                    $ref: '#/components/schemas/AuthTokens'
        '400':
          description: Invalid tenant id
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/v1/users:
    get:
      operationId: listUsers
      summary: List users
      description: Get a paginated list of users with optional search and sorting
      tags:
        - Users
      parameters:
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/perPageParam'
        - $ref: '#/components/parameters/sortByParam'
        - $ref: '#/components/parameters/orderParam'
        - $ref: '#/components/parameters/searchParam'
      responses:
        '200':
          description: User list retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Pagination'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

    post:
      operationId: createUser
      summary: Create user
      description: Create a new user account (requires the global `users.create` permission)
      tags:
        - Users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - full_name
                - email
                - password
                - password_confirmation
              properties:
                full_name:
                  type: string
                email:
                  type: string
                  format: email
                username:
                  type: string
                password:
                  type: string
                  format: password
                  minLength: 8
                password_confirmation:
                  type: string
                  format: password
      responses:
        '201':
          description: User created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/users/{id}:
    get:
      operationId: getUserById
      summary: Get user by ID
      description: Retrieve a specific user's details
      tags:
        - Users
      parameters:
        - $ref: '#/components/parameters/pathId'
      responses:
        '200':
          description: User details retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'

    put:
      operationId: updateUser
      summary: Update user
      description: Update user details (email and username cannot be changed)
      tags:
        - Users
      parameters:
        - $ref: '#/components/parameters/pathId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                full_name:
                  type: string
                password:
                  type: string
                  format: password
                  minLength: 8
                password_confirmation:
                  type: string
                  format: password
                  description: Required if password is provided
      responses:
        '200':
          description: User updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '422':
          $ref: '#/components/responses/ValidationError'

    delete:
      operationId: deleteUser
      summary: Delete user
      description: Soft delete a user (marks as deleted)
      tags:
        - Users
      parameters:
        - $ref: '#/components/parameters/pathId'
      responses:
        '204':
          description: User deleted successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'

  /api/v1/admin/roles:
    get:
      operationId: listRoles
      summary: List roles
      description: Get a paginated list of roles (requires ADMIN or ROOT role)
      tags:
        - Roles
      parameters:
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/perPageParam'
      responses:
        '200':
          description: Role list retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Pagination'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Role'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/v1/admin/roles/attach:
    put:
      operationId: attachRoles
      summary: Attach roles to user
      description: Assign roles to a user (requires ADMIN or ROOT role)
      tags:
        - Roles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - user_id
                - role_ids
              properties:
                user_id:
                  type: integer
                  example: 1
                role_ids:
                  type: array
                  items:
                    type: integer
                  example: [1, 2, 3]
      responses:
        '200':
          description: Roles attached successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: 'Role attached successfully'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/permissions:
    get:
      operationId: listPermissions
      summary: List permissions
      description: Get a paginated list of permissions
      tags:
        - Permissions
      parameters:
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/perPageParam'
      responses:
        '200':
          description: Permission list retrieved successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      operationId: createPermission
      summary: Create permission
      description: Create a new permission
      tags:
        - Permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - resource
                - action
              properties:
                resource:
                  type: string
                  example: 'posts'
                action:
                  type: string
                  example: 'create'
                description:
                  type: string
                  example: 'Allow creating posts'
      responses:
        '201':
          description: Permission created successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/roles/permissions/sync:
    put:
      operationId: syncRolePermissions
      summary: Sync role permissions
      description: Sync all permissions for a specific role
      tags:
        - Permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role_id
                - permission_ids
              properties:
                role_id:
                  type: integer
                  example: 2
                permission_ids:
                  type: array
                  items:
                    type: integer
                  example: [1, 2, 3]
      responses:
        '200':
          description: Permissions synced successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/roles/permissions/attach:
    put:
      operationId: attachRolePermissions
      summary: Attach permissions to role
      description: Attach one or more permissions to a role
      tags:
        - Permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role_id
                - permission_ids
              properties:
                role_id:
                  type: integer
                  example: 2
                permission_ids:
                  type: array
                  items:
                    type: integer
                  example: [4, 5]
      responses:
        '200':
          description: Permissions attached successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/roles/permissions/detach:
    put:
      operationId: detachRolePermissions
      summary: Detach permissions from role
      description: Detach one or more permissions from a role
      tags:
        - Permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role_id
                - permission_ids
              properties:
                role_id:
                  type: integer
                  example: 2
                permission_ids:
                  type: array
                  items:
                    type: integer
                  example: [1]
      responses:
        '200':
          description: Permissions detached successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/users/permissions/sync:
    put:
      operationId: syncUserPermissions
      summary: Sync user permissions
      description: Sync all direct permissions for a specific user
      tags:
        - Permissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - user_id
                - permission_ids
              properties:
                user_id:
                  type: integer
                  example: 3
                permission_ids:
                  type: array
                  items:
                    type: integer
                  example: [10, 11]
      responses:
        '200':
          description: User permissions synced successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/admin/users/{id}/permissions:
    get:
      operationId: getUserPermissions
      summary: Get user's direct permissions
      description: Get a list of direct permissions for a specific user
      tags:
        - Permissions
      parameters:
        - $ref: '#/components/parameters/pathId'
      responses:
        '200':
          description: User permissions retrieved successfully
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/v1/admin/users/{id}/permissions/check:
    post:
      operationId: checkUserPermissions
      summary: Check user permissions
      description: Check if a user has specific permissions
      tags:
        - Permissions
      parameters:
        - $ref: '#/components/parameters/pathId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - permissions
              properties:
                permissions:
                  type: array
                  items:
                    type: string
                  example: ['users.create', 'files.delete']
      responses:
        '200':
          description: Check result
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          $ref: '#/components/responses/ValidationError'

  /api/v1/files:
    get:
      operationId: listFiles
      summary: List files in the active workspace
      description: Returns a paginated tenant-scoped file list. Requires `files.list` and an active workspace.
      tags:
        - Files
      parameters:
        - $ref: '#/components/parameters/tenantHeader'
        - $ref: '#/components/parameters/pageParam'
        - name: per_page
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: Workspace file list
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                      perPage:
                        type: integer
                      currentPage:
                        type: integer
                      lastPage:
                        type: integer
                      firstPage:
                        type: integer
                      nextPageUrl:
                        type: [string, 'null']
                      previousPageUrl:
                        type: [string, 'null']
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/FileListItem'
        '400':
          description: An active workspace is required or the tenant header is malformed
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/v1/files/upload:
    post:
      operationId: uploadFile
      summary: Upload file
      description: |
        Upload a file to the server. Files are automatically categorized based on their type.

        **File constraints:**
        - Maximum size: 10MB
        - Allowed extensions: jpeg, jpg, png, pdf, doc, docx, txt, csv, xls, xlsx, mp3, mp4, zip

        **Categories:**
        - `image`: jpeg, jpg, png
        - `document`: pdf, doc, docx, txt
        - `video`: mp4
        - `audio`: mp3
        - `file`: csv, xls, xlsx, zip, and others
      tags:
        - Files
      parameters:
        - $ref: '#/components/parameters/tenantHeader'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: File to upload
      responses:
        '201':
          description: File uploaded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileUploadResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '422':
          description: File validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                errors:
                  - message: 'File size should be less than 10MB'
                    field: 'file'
                    rule: 'size'

  /api/v1/files/{id}:
    delete:
      operationId: deleteFile
      summary: Delete a workspace file
      description: Deletes a file from storage and the database. `files.delete` may delete any workspace file; `files.delete.own` is restricted to the uploader.
      tags:
        - Files
      parameters:
        - $ref: '#/components/parameters/pathId'
        - $ref: '#/components/parameters/tenantHeader'
      responses:
        '204':
          description: File deleted successfully
        '400':
          description: An active workspace is required or the tenant header is malformed
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
