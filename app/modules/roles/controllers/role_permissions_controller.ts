// Backward-compatible module alias. Permission administration is owned by the
// permissions domain and has a single controller/validation boundary.
export { default } from '#modules/permissions/controllers/permissions_controller'
